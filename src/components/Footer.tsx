import React from 'react';

import styles from './Footer.module.css';

function Footer() {
    return (
        <footer className={ styles.footer }>
            <p>Created to make it easier to build web applications with Next.js.</p>
        </footer>
    )
};

export default Footer;